{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}

<div class="container check_email">
    <p>
        Si un compte correspondant à votre adresse e-mail existe, alors un e-mail vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
        Ce lien expirera dans 1 heure .
        
        {# {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} #}
    </p>
    <p>Si vous ne recevez pas d'e-mail, merci de vérifier vos spam ou <a href="{{ path('app_forgot_password_request') }}">réessayez</a>.</p>


	<a href="{{ path('app_home')}}" class="btn btn-primary mt-5">Retourner à la page d'accueil<a>

</div>
{% endblock %}
